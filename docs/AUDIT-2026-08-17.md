# Gridlink Mail interaction audit, 2026-08-17

Driven against a clean-slate install on an Android 16 emulator (`pm clear`, true first-run),
package `app.gridlink` v0.1.0 (versionCode 1000). Every item below was reproduced on-device and
then root-caused in source. Items marked CODE were additionally confirmed by reading the code.

> **Status, updated as the plan is worked.** Items 1, 2, 3, 4, 6 and 7 are FIXED and pushed.
> Item 5 was re-scoped after it turned out to be partly wrong, and what was left of it is now
> closed as won't-fix by Tate's call (see item 6). **Items 8 and 9 are the only open ones** (9 is half done: the R8 release build is proven on the emulator, the phone install is the remaining step).
> Item 10 stays deferred: F-Droid MR !46061 was still open with no maintainer comment when
> checked on 2026-08-18.
>
> ✅ **Found while fixing item 6, and resolved separately.** `GridlinkSetupScreen`'s KDoc said the
> Calendar and Contacts sync toggles "default to on because 'set up my account' means the account",
> while both were initialised `false` and rendered OFF. The code had been that way since the screen
> was added (`96ce306e`); the doc claim arrived later, when the toggles stopped being decorative.
> Tate's call, 2026-08-18: **the doc was right, the code is now `true`.** Both are one tap away
> on the same screen before anything is fetched. Verified ON at first run on the emulator.
>
> 🔴 **Attribution correction.** Items 3, 5 and 6 name `ConnectScreen.kt`. That is the ADVANCED
> setup screen, reached only by tapping "JMAP, IMAP or Outlook". The screen actually photographed
> at first run is `GridlinkSetupScreen.kt`, reached through `GridlinkSetupHost` (which calls
> `GridlinkApp {` as a trailing lambda, which is why an earlier grep for `GridlinkApp(` missed it).
> The shared button lives in `GridlinkForm.kt`.

## Confirmed defects, ranked

### 1. HIGH / security: the mail password is written into the saved-state Bundle
`ConnectScreen.kt:142` and `:938` hold it as `var password by rememberSaveable { mutableStateOf("") }`.
`rememberSaveable` serialises into the Activity's `onSaveInstanceState` Bundle, which is held by
system_server outside the app process and may be persisted for task restoration. A plaintext mail
credential should never reach it. CODE + reproduced (password restored after forced recreation).
**Fix:** hold the password in the ViewModel (survives config change, never serialised), not in
saveable state.
✅ **FIXED** in `9cbca192`: `ConnectViewModel.PasswordDrafts` holds both the form password and the
per-account import password, cleared once stored. Survives the Fold's hinge, dies with the process.

### 2. HIGH / visual: light mode renders an unreadable status bar
`Theme.kt:56` sets `isAppearanceLightStatusBars = !darkTheme`, so in light mode the status-bar
icons go dark. But the Gridlink-skinned screens are dark-only: `GridlinkSolar.kt` and
`GridlinkTokens.kt` contain zero references to `isSystemInDarkTheme` / `ThemeMode` / `darkTheme`.
Result: dark icons on a dark navy surface, barely legible. The code comment asserts "they stay
legible on the light surface"; on these screens no light surface exists.
**Repro:** `adb shell cmd uimode night no`, open the app, look at the clock. CODE + screenshot.
**Fix:** drive the system-bar appearance from the surface actually being drawn, not from the
Material theme mode.
✅ **FIXED** in `71e4468f`: `AppTheme` stays the single writer of the bar appearance, and
`ProvideGridlinkTokens` publishes the mode it paints into a slot `AppTheme` reads during
composition. Pinned by `SystemBarsAppearanceTest`.

### 3. MEDIUM / accessibility: the disabled Connect button disappears from the a11y tree
With the form empty, Connect is correctly disabled (`ConnectScreen.kt:583`, `enabled = !busy && ready`),
but its clickable node `[1041,2796][1380,2988]` is ABSENT from the uiautomator tree; only a bare
`TextView` reading "Connect" remains, reporting `enabled=true`. A screen-reader user is read static
text with no role and no reason it is unavailable.
**Fix:** keep the button in the semantics tree when disabled and attach a stateDescription saying
what is still required.
✅ **FIXED** in `5f4e599a`. Root cause was `GridlinkConfirmPill` in `GridlinkForm.kt`, which applied
`.clickable` only inside the `if (enabled)` branch, so the Box contributed no semantics node at all
when disabled. `clickable(enabled = false)` keeps the node, publishes the role and marks it
unavailable. The screen's live `hint` now rides along as the pill's `stateDescription`.
`GridlinkTextButton` in `GridlinkSheets.kt` had the identical defect and is fixed with it.

### 4. MEDIUM / UX: notification permission is requested at cold start, and re-requested
The POST_NOTIFICATIONS dialog fires on first launch before any account exists and before the user
has done anything. It fired again after an activity recreation despite an earlier explicit denial.
For a privacy-focused F-Droid audience this is the wrong first impression.
**Fix:** request in context, at the point the user enables push or sync, and never re-prompt after
a denial.
✅ **FIXED** in `d92f640c` and `ee8e7ec2`. The ask moved out of the top of `AppNavHost` into the
`RootState.Authenticated` branch, so it happens once a mailbox exists, and the fact that it was
asked is recorded on disk so a denial is not re-asked every cold start. Because a denial is now
permanent, the notifications settings screen grew a row, shown only while the OS is blocking, that
opens the system notification settings.

### 5. ~~MEDIUM~~ LOW / UX: the empty form is a silent dead end
🔴 **Half of this was wrong and is withdrawn.** "Nothing states what is missing" is not true of the
real first-run screen: `GridlinkSetupScreen.kt:147` computes a live `hint` that names exactly the
next thing needed ("Enter the address you receive mail at, like you@yourdomain.com.", then "Enter
the password, app password or API token for that account."), and `GridlinkFormScreen` renders it.
I mistook that changing hint for a static subtitle.

What survives is the affordance: disabled is drawn as a hairline outline, which is a deliberate
house rule (alpha never encodes state) rather than an oversight, but it reads as chrome rather than
as a button waiting on you. ✅ The accessibility half is closed by item 3, which now speaks the hint
on the button itself. The remaining visual question is folded into item 6, since both are about how
this one screen reads.

### 6. LOW / UX: the optional field leads the required one
The subtitle says "Enter the address you receive mail at", then the first field is SERVER, marked
optional. ACCOUNT (the thing just asked for) is second. The reading order contradicts the instruction.
**Fix:** put ACCOUNT first, SERVER after, or fold SERVER into an "advanced" disclosure.
✅ **FIXED** in `GridlinkSetupScreen.kt`. Tate's call, 2026-08-18: **swap the sections, do not
collapse SERVER behind a disclosure.** The screen now reads ACCOUNT (address, username, password,
show password) → SERVER (optional) → SYNC, so the subtitle names the field it sits above. SERVER
keeps its own section label and stays in plain sight one screenful down; the brief's objection was
to *hiding* the server, and it is not hidden.

Two details that came with it:
- The password row keeps `ImeAction.Done` rather than advancing into SERVER. Next would march every
  user into the optional field, which is the same reading-order complaint moved down a row. SERVER
  is now the end of the chain and is `Done` too.
- Initial focus still goes to the address, which is now also the first field on screen, so the
  keyboard opens on the thing the hint is asking for.

**Verified on the Android 16 emulator** after `pm clear` (true first run): visual order confirmed by
screenshot, and the uiautomator tree lists the nodes in the same order. Item 3 re-checked on the same
dump and still holds: the disabled Connect node is present at `[1755,1869][2027,2025]` with
`clickable="true" enabled="false"`. `detekt` and the unit suite pass.

**The leftover visual half of item 5 is CLOSED as won't-fix, by Tate's call.** The disabled
Connect pill stays a hairline outline: the house rule (alpha never encodes state) stands, and the
accessibility half is already covered by item 3, which speaks the live hint on the button itself.

### 7. LOW: credential restoration is inconsistent
The password survived one forced recreation and was cleared by another (uiMode change). Whatever
the intent, the behaviour is not predictable. Resolving item 1 should make this deterministic.
✅ **CLOSED by item 1.** The lifetime is now stated rather than incidental: the draft lives in the
ViewModel, so it survives every configuration change and no process restart.

## Coverage gaps (from the static audit, unchanged)

### 8. Zero UI / instrumented tests; 28 of 29 screens and view models uncovered
1,622 unit tests pass and the protocol layer is well proven. Nothing covers the layer the user
touches. Every defect above was found by hand in one session because no test could have caught them.

▶ **Started, 2026-08-20: the first screen tests exist, and they run without a device.**
- `GridlinkSetupScreenTest` (13 cases) and `WelcomeScreenTest` (2) under `app/src/test`, driven
  by Compose's own test rule and hosted on the JVM by Robolectric, so they are part of
  `./gradlew test` and of any CI that runs it. ⛔ Not an `androidTest` source set, on purpose: a
  suite that needs an emulator attached is a suite that does not run, and not running is exactly
  how this list came to exist.
- Every fixed item above that lives in the setup screen now has a regression test: the disabled
  Connect pill stays a button with its reason on it (3), the empty form names what is missing (5),
  the address field leads and the server trails (6), the password is dropped by a saved-state
  restore while address / login / server survive it (1 and 7, via `StateRestorationTester`), the
  sync toggles start ON and an OFF tap reaches the request, the password is masked until Show
  password, busy locks the pill and the advanced link, an error replaces the hint and the attempt
  log expands on request.
- Wiring: `testOptions.unitTests.isIncludeAndroidResources = true`, `ui-test-junit4`, `robolectric`
  4.15.1, `ui-test-manifest` (debug only), and `src/test/resources/robolectric.properties` pinning
  `sdk=35`, a plain `android.app.Application`, and a phone-sized window. The window matters:
  on Robolectric's default display the lower rows are clipped and taps aimed at them land on
  nothing, so tests scroll a row into view before tapping it.
- Same day, the two other forms built on `GridlinkFormScreen`: `GridlinkEventFormScreenTest` (9)
  and `GridlinkContactFormScreenTest` (7). The event form's caller contract is now enforced (a new
  event goes out with an empty id, an edit keeps its id / domain / handle, all-day means no times
  rather than midnight, moving the start past the end drags the end along, reminders are toggled
  in a sheet that stays open until Done); the contact form's is too (exact words, spare rows
  included, a name or company before it can be filed, the composer's own address matcher gating
  the save). The pickers are real dialogs and the test rule reaches into them, so the time sheet
  and reminder sheet are driven by tap like the form is.
- Also `GridlinkSnoozeScreenTest` (6, including the snooze sheet) and `GridlinkScheduledScreenTest`
  (5): an empty account says so instead of drawing the sample, rows come soonest first whatever
  order the flow handed them in, Wake now / Cancel send hand back the key of the row tapped, Back
  closes once, and the sheet reports the preset's millis and leaves closing to its caller.
- Same day, the two big pure screens: `GridlinkThreadScreenTest` (18) and
  `GridlinkFolderScreenTest` (13). The reader's contract with its host is pinned end to end: every
  control maps to exactly one `GridlinkThreadAction` or named callback, the bar draws only the
  enabled `ThreadToolbarAction`s with More holding the rest, Star reports the state it moves TO
  and shows it before the mailbox confirms, Unsubscribe is asked first and reaches the caller once
  on confirm, the remote-images banner blocks until Show (local) or Always (the caller's list),
  an attachment chip opens or saves THAT file, invite buttons send their PARTSTAT, the receipt row
  sends once. The WebView body is a Robolectric shadow and nothing around it reads it, so no
  stubbing. The folder tree: the caller owns the tree and the screen redraws from what it is
  handed, the header counts nested mailboxes and says Loading rather than "0 mailboxes", chevrons
  alone open branches, a row tap opens that folder only, the inbox refuses a long-press while the
  other role mailboxes get a watch-only sheet and user folders get the full one, rename refuses a
  sibling's name case-insensitively and trims, delete is inert with the reason while children
  remain, the inline New folder row creates at its own level and names the parent (null at the
  root), the nav pill and New message report to the caller. Writing them found two things in the
  screen, both fixed in the same commit: the header KDoc still said role mailboxes never answer a
  long-press (stale since "Notify me here"), and the `initialCreateUnder` screen-capture seed
  resolved its ancestors against the SAMPLE tree, the one lookup that escaped the "resolve against
  `tree`" fix (harness-only, a real account's nested create target simply produced no field).
- Same day, the two detail cards: `GridlinkEventScreenTest` (13) and `GridlinkContactScreenTest`
  (10). The event card: the day in full and the span with its duration, all-day says so and draws
  no section it has nothing for, Edit only with a writer and handing back the same event,
  reminders sorted on one line, HTML notes rendered not printed, attachment chips open THAT file
  and Remove asks first and only when the server said removable, "Also that day" lists the book's
  other events with all-day first and opens the one tapped, the outside party is named from the
  LIVE book (organisation over person, domain alone when unknown) and a live calendar lists no
  sample mail, Copy puts the card on the clipboard without the reminders. The contact card: hero,
  embedded drops the duplicate title, an organisation card is named by the organisation, Copy /
  Write / Edit, every detail row in order and Company only when it adds something, an email row
  writes to THAT address, the phone row dials and the address row asks the map (checked through
  Robolectric's next started activity), the sample contact's recent mail opens the message tapped.
  Writing the contact test found a real one, fixed in the same commit: the card's "Recent mail"
  panel read `GridlinkSample.messagesFrom` UNCONDITIONALLY, so on a real address book every
  contact would read "Nothing from this address yet.", a confident wrong statement about somebody
  who may have written. It is now gated on `GridlinkBook.contactsLive` exactly like the event
  card's mail panel (absent on a live book, not empty) and the header says why. Also tidied: an
  orphaned KDoc in the event screen that had drifted off `asPlainText`, and detekt's line-length
  rule now runs over the new test files (it had not been run on batches 4 and 5; four lines wrapped).
- Batch 7, same day: the two list screens, `GridlinkCalendarScreenTest` (11) and
  `GridlinkContactsScreenTest` (10). Neither takes data as a parameter (both read
  `LocalGridlinkBook`), so the tests provide a hand-built book through `CompositionLocalProvider`
  with invented names, and one test per screen pins the sample fallback. Calendar: opens on today's
  month and counts it, the four tabs and their titles, steppers page the month and the count follows
  and a month paged to is where the week lands, tapping a day lists it and reports the date, events
  open from the day list and the week grid, the agenda opens at today with no steppers and lists
  free days as "Nothing scheduled", "+" hands back the selected day in the month and the anchor in
  the other views, and the header says "Loading" or "Not synced this far out" rather than a
  confident zero. Contacts: grouped by filed letter in phonebook order with the header counting
  every card, the second line falls back role then email then phone, the name-order pill regroups
  and saves the choice, `initialScrubLetter` opens jumped to that letter (or the nearest populated
  one before it), tapping a row opens that card, empty and loading states. Two real things came out
  of it, both fixed in the same commit: the name-order pill's scroll-to-top ran from the tap,
  BEFORE the recomposition that regroups the list, so the lazy list measured the old list and then
  kept the old first item by key; flip Last,First to First Last and back and it sat at the "C"
  heading with A and B scrolled off above (now a `LaunchedEffect` keyed on the flip, and the test
  asserts "A" is displayed without scrolling); and the calendar header printed "1 events". Also
  corrected the `onNewEvent` KDoc, which claimed the week view hands back the first day on screen
  when it hands back the anchor (any weekday of the row shown), pinned as built by the test.
- Batch 8, same day: `GridlinkFolderMailScreenTest` (24), the per-folder mail list. Covered: the
  sample fallback when nobody is supplying mail (no skeleton, no empty state, no long-press), real
  mail under the TODAY/YESTERDAY/EARLIER headings in the enum's order whatever order it arrived in
  (a robot's mail sits under a day here, no Automated bundle), mail for another folder shows the
  skeleton rather than the wrong list, the empty state names the mailbox, the header counts unread,
  tap opens the message, the long-press sheet and every line on it (Archive hidden in Archive,
  "Mark spam" hidden in Junk, "Move to" only when wired, Mark read/unread follows the row's state,
  Delete's Trash subline says it is for good), delete and archive hide the row while read-state
  actions keep it, a move hands the message over and keeps the row, and Empty: offered on Trash
  only, only when there is mail and a handler, asks first, Cancel does nothing and the confirm
  fires once. Nothing in production needed changing. One fixture lesson: `gridlinkFolderSection`
  reads a clock suffix before the day word, so a stamp like "Yesterday 4:10 PM" files under TODAY;
  real mail never hits this (it carries an explicit section and `MailDates` stamps a bare
  "Yesterday"), but a test fixture can.
- Batch 9, same day: the first view-model-bound screen, Filters. `FiltersScreen` was split into
  a thin view-model wrapper and `FiltersScreenContent(state, callbacks)`, the same shape every
  other tested screen already had, so the screen can be driven with hand-built state and no
  server; nothing about what it draws changed. `FiltersScreenTest` (16): every state the view
  model can put it in (loading, no account, unsupported server, load failure with Retry, empty
  list, foreign active script warning), rows named or "Untitled rule" with the one-line summary,
  the row switch reporting index and target state, Save offered only when dirty and hidden while
  saving, the saved confirmation and the save failure, Add rule opening the editor on the new rule
  and Back handing over every field typed (name, field, condition, value, move target, Star), a
  tapped row opening seeded and Delete handing back its index, a rule aimed at a folder no longer
  offered keeping its target, and leaving: clean leaves at once, dirty asks first (Cancel stays,
  Discard leaves), Save-then-leave waits for the round-trip, stays on a refusal and leaves once
  clean. `FiltersViewModelTest` (6) pins the bookkeeping against the real container: no account
  is reported as such and not as a load failure, a blank rule alone is dirty, an untouched rule
  committed from the editor is dropped, edits replace in place, remove and toggle hit the row
  named, Save with no account is a no-op.
  Wiring for this and the view-model screens to come: `TestGridlinkApplication` (src/test) is
  the real `GridlinkApplication` with a synchronous test WorkManager stood up first, because the
  container's init schedules the fallback poll and under Robolectric the library's startup
  provider does not run (`work-testing` added, `GridlinkApplication` opened for it).
  `AppContainerStandsUpTest` is the canary: if the container cannot be built on the JVM, look
  there first.
- Batch 10, same day: `ConnectScreenTest` (17), the add-account form, driven for real against
  `ConnectViewModel` and the app container through `TestGridlinkApplication`; no production
  change and no split, since the form is plain local state and the view model answers two of its
  paths without a server. Covered: the first-run welcome and its absence when adding a second
  account, the import doors, Back; JMAP as the default with Password selected, the server field
  hidden until Advanced and hidden again after; Connect waiting for email plus password while the
  OAuth button needs only the email; API token relabelling the secret field and dropping the
  OAuth route, and Password bringing both back; a malformed address flagged while typing and
  cleared once whole; a Fastmail address steered to the token option until it is chosen; the
  password eye; IMAP / SMTP revealing the server block with the form defaults (993 and 465, both
  SSL/TLS) and Connect waiting for both hosts; a provider row filling the block (iCloud: STARTTLS
  on 587, no app-password link; Gmail: 465, the link), plain IMAP clearing what a provider filled,
  the security chips switching only their own row; Outlook hiding the server block and arming
  Connect on the address alone, then JMAP disarming it. The two offline paths, with Robolectric's
  stand-in networks removed: JMAP autodiscovery and IMAP both refuse before touching the network,
  the error names the missing connection, Show details reveals "Checking this device's network:
  no connection" and Connect is live again. Outlook's Connect is armed but never pressed, since
  this build carries the real Microsoft client id and it would start the device flow. One harness
  fact: the form scrolls and Robolectric's display is short, so every tap scrolls its target into
  view first (`performScrollTo().performClick()`); a tap below the fold lands on nothing and the
  test reads as a missing node. `ConnectPrimingWiringTest`'s note that the view model cannot be
  instantiated in this module was true until batch 9 and is reworded.
- Batch 11, same day: `TagsScreenTest` (7) and `LicenseScreenTest` (2). The tag manager is
  driven for real against `SettingsViewModel` and the container's settings store: tag definitions
  live in local DataStore, so create, rename and delete round-trip with no server. Covered: the
  empty state and its wording, the editor's wire-name preview ("Sent to the server as
  receipts-2026") and its Save gate (disabled until a label slugs to something, and again for
  punctuation alone), Cancel creating nothing, a saved tag listing with its keyword underneath, a
  rename keeping the keyword the mail already carries (and never re-deriving it from the new
  label), Delete asking first and saying that mail keeps the tag, Cancel keeping it and Delete
  removing it back to the empty state, and Back. "Seen on your mail" needs cached mail on an
  account and is asserted absent on a fresh install. The licence page shows the bundled GPL text
  verbatim (polled, since it is read off the IO dispatcher) and Back leaves. One production
  change: the tag editor focused its name field from a `LaunchedEffect`, which under Robolectric
  throws "FocusRequester is not initialized" because a dialog composes before its window has
  attached; the rename-folder dialog's wait-a-frame trick loses the same race when the dialog
  opens mid-screen, so the editor now requests focus from the field's first layout pass, which
  cannot happen before the field is attached. Three harness facts, all written into the test:
  section titles render uppercased ("YOUR TAGS"); a stock Material 3 `AlertDialog` with a
  `fillMaxWidth` field never settles on Robolectric's narrow default display (the wrap-content
  window is offered a different width every frame and re-measures until the JVM runs out of heap,
  which then takes every later test in the run with it), so the class pins a display wider than
  the dialog's 560dp cap (`w800dp-h1280dp`, also the Fold's inner screen); and the settings
  DataStore is a process-wide singleton, so tags saved by one test are still there for the next
  and every test starts by wiping them through the repository.
- Batch 12, same day: `SettingsScreenTest` (29), the settings hub and its pages, driven for real
  against `SettingsViewModel`, `AccountsViewModel` and `StorageViewModel` on the container's
  DataStore. Covered: the hub lists every door with its summary line, Version and Source code are
  inert while the rest open, each page opens from its row and Back returns to the hub (only the
  hub's own Back leaves, and a deep link into a page leaves on Back instead of showing the hub);
  Appearance at its defaults, the language dialog's choices and Cancel, density and preview lines
  changing and reading back, Group automated mail, and the icon choice explaining Auto and OLED
  and actually swapping the launcher alias (`PackageManager` reports `icon.Dark` enabled and
  `icon.Light` disabled); Reading at its defaults, text size, "Mark as read when" naming the
  chosen moments in the list's own order, swipe right offering every action, and the thread-action
  and signature switches; Notifications at its defaults, delivery and content dialogs, quiet
  hours revealing a 10:00 PM to 7:00 AM window with a picker behind Start, push for all accounts;
  Privacy at its defaults, App lock saying so and staying off with no device lock and turning on
  with one, the link switches, the remote-image allowlist adding, removing and clearing senders
  (Cancel adds nothing), and Suggest from contacts turning on once READ_CONTACTS is granted; a
  fresh install's Accounts page offering only Add account (which opens the form), Storage showing
  the three usage rows and Clear cache asking first, Backup offering export and import. One
  production change: the add-sender dialog focused its field from a `LaunchedEffect`, the same
  race the tag editor had in batch 11 (the dialog opens mid-screen, the effect runs before the
  field is attached, "FocusRequester is not initialized"), so it now requests focus from the
  field's first layout pass. Two harness facts worth keeping: DataStore runs each `edit`
  transform on the caller's context, which for the view model is the main dispatcher, so every
  settings write posts a task back to the main looper from DataStore's own thread, and under
  Robolectric the Compose rule does not drain tasks that arrive that way once it is idle (a second
  write in a test sat queued until some later tap happened to idle the looper), so every poll in
  the test idles the main looper before checking; and Robolectric's `BiometricManager` answers
  "can authenticate" by default, so the no-device-lock branch is reached through
  `ShadowBiometricManager.setCanAuthenticate(false)` (via `Shadow.extract`, there is no
  `shadowOf` overload for it). The allowlist is a `Set`, so the test finds a sender's Remove
  button by its row rather than by position.
- Still open: 8 of 29 screens and view models: `GridlinkComposeScreen`, `GridlinkMessageListScreen`,
  `LockScreen`, the account-detail and vacation pages inside Settings (both need a signed-in
  account; `VacationViewModel`), `GridlinkMailViewModel`, `GridlinkDavViewModel`, and
  `ConnectViewModel` on its own (it is driven only through the form so far). Item 2 (status bar)
  and item 4 (when the notification ask fires) are not covered here, since one is a window effect
  and the other lives in the nav host behind a real permission launcher.

### 9. The phone runs a debug build, so the release path is never exercised
The Fold carries a debug-keystore-signed APK. The minified R8 release build that F-Droid ships is
never run on real hardware. See [[gridlink-mail-audit-2026-08]].

▶ **Half done, 2026-08-20.** The release build was exercised end to end, just not yet on the phone:
- `:app:assembleRelease` with the real `keystore.properties` key produced a single-`classes.dex`
  APK (6.16 MB, R8 ran, signer `CN=Gridlink Mail`). Installed as a true first run on a clean
  Android 16 `fold` AVD (`emulator-5560`) and driven by hand against the disposable test mailbox
  `avery@gridlink.me`: SRV discovery -> JMAP sign-in, the notification ask arriving only AFTER
  auth (item 4 holding in release), inbox sync and unread counts, reading pane with
  Archive / More / Reply, Calendar month + agenda + event card, Contacts list + card, search with
  body snippets, drawer folders and Drafts, Settings. Fonts, icons and layout all intact, no
  `app.gridlink` entry in `logcat -b crash`. So R8 shrinking and resource stripping break nothing
  visible in the main flows.
- ⚠️ R8 prints 123 copies of `WARNING: R8: An error occurred when parsing kotlin metadata` under
  AGP 8.13.2 + Kotlin 2.4.10. The build exits 0 and the app runs; it is the known R8-older-than-
  Kotlin mismatch, not a defect, but it will keep appearing until AGP's bundled R8 catches up.
- ⏳ **Still open: the same build on the Fold itself.** The `-PphoneKey` release build (identical
  bytes apart from the signature) is staged; installing it over the phone's debug build needs a
  hand on the keyboard. Rollback is the pulled debug APK (`adb install -r`, same signature).

### 10. F-Droid listing is English-only despite 8 translated locales
`fastlane/metadata/android/` holds `en-US` only. DEFERRED: fastlane metadata is read at the tag,
so fixing it means re-tagging, which would move the ground under merge request !46061 while a
maintainer is reviewing it. Do this after the MR merges.

## Verified NOT broken (do not re-audit)

- State survives backgrounding and forced activity recreation, with no crash.
- Connection error copy is clear and actionable ("No JMAP server was found for that address"),
  with Show details / Copy details affordances. Better than most clients.
- The grey floating circle visible in some screenshots is a system overlay, not Gridlink: it has no
  node in the accessibility tree and the only package in the dump is `app.gridlink`.
- Zero crashes across the whole session (`logcat -b crash` clean).
