<?xml version="1.0" encoding="utf-8"?>
<!--
  The resizable "recent mail" widget.

  🔴 updatePeriodMillis is 30 minutes, which is the floor the platform honours — anything smaller
  is silently rounded up to 30 minutes, so writing 5 would promise a refresh rate that cannot
  happen. It is a backstop, not the mechanism: the widget's real refresh is a poke from the sync
  that just wrote new mail (GridlinkWidgets.refresh), and this timer only covers the case where
  nothing has synced but the relative times on screen have gone stale.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="180dp"
    android:minHeight="110dp"
    android:minResizeWidth="180dp"
    android:minResizeHeight="110dp"
    android:targetCellWidth="4"
    android:targetCellHeight="3"
    android:updatePeriodMillis="1800000"
    android:initialLayout="@layout/widget_inbox"
    android:previewLayout="@layout/widget_inbox"
    android:previewImage="@mipmap/ic_launcher"
    android:description="@string/widget_inbox_description"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen" />
