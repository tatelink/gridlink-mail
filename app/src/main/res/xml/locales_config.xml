<?xml version="1.0" encoding="utf-8"?>
<!-- Languages Jmail ships translations for. Surfaced in the system per-app
     language picker (Settings → Apps → Jmail → Language) and read by AppCompat. -->
<locale-config xmlns:android="http://schemas.android.com/apk/res/android">
    <locale android:name="en" />
    <locale android:name="fr" />
</locale-config>
