<?xml version="1.0" encoding="utf-8"?>
<!--
  One message in the widget list. Deliberately a MESSAGE and not a thread: the reader behind this
  is flat and uncollapsed, so a five-row widget shows five messages rather than two conversations.

  The whole row is one tap target, filled by the collection's PendingIntent template. Nothing
  inside it is separately clickable — a row in a widget is small enough that a second target
  inside it is a mis-tap waiting to happen.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/row_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:background="@drawable/widget_row"
    android:padding="10dp"
    android:layout_marginBottom="6dp"
    android:baselineAligned="false">

    <!-- Unread marker. Hidden (GONE) on read mail rather than drawn in a faded colour: a dot
         that is always present in some form reads as decoration, not as state. -->
    <ImageView
        android:id="@+id/row_unread"
        android:layout_width="8dp"
        android:layout_height="8dp"
        android:layout_marginTop="5dp"
        android:layout_marginEnd="8dp"
        android:src="@drawable/widget_unread_dot"
        android:contentDescription="@string/widget_unread_marker" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:baselineAligned="true">

            <TextView
                android:id="@+id/row_sender"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:textColor="@color/widget_row_text"
                android:textSize="13sp"
                android:maxLines="1"
                android:ellipsize="end" />

            <ImageView
                android:id="@+id/row_attachment"
                android:layout_width="14dp"
                android:layout_height="14dp"
                android:layout_marginStart="6dp"
                android:src="@drawable/ic_widget_attachment"
                android:contentDescription="@string/widget_has_attachment" />

            <!-- Fixed-width so the times form a column instead of ragging with the sender name. -->
            <TextView
                android:id="@+id/row_time"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="6dp"
                android:textColor="@color/widget_row_text_secondary"
                android:textSize="11sp"
                android:maxLines="1" />
        </LinearLayout>

        <TextView
            android:id="@+id/row_subject"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="1dp"
            android:textColor="@color/widget_row_text"
            android:textSize="13sp"
            android:maxLines="1"
            android:ellipsize="end" />

        <TextView
            android:id="@+id/row_preview"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="1dp"
            android:textColor="@color/widget_row_text_secondary"
            android:textSize="12sp"
            android:maxLines="1"
            android:ellipsize="end" />
    </LinearLayout>
</LinearLayout>
