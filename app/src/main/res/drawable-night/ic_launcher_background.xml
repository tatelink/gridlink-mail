<?xml version="1.0" encoding="utf-8"?>
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <!-- The Gridlink tile, dark mode. Flat #06070a, the Forge page background, so the mail
         icon reads as the same family as Forge and the webmail favicon. No gradient on
         purpose: the canonical master in ~/homelab/mail/branding/gridlink-icon.svg is flat,
         and this is the one place the whole icon family has to agree.

         The launcher mask supplies the corner radius, so this is a plain full-bleed square.
         See drawable/ic_launcher_background.xml for the light-mode twin. -->
    <path
        android:fillColor="#FF06070A"
        android:pathData="M0,0h108v108h-108z" />
</vector>
