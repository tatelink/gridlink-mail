JMAP accounts can now sign in with an API token (works with Fastmail). Replying, forwarding and quick-replying on IMAP accounts is fixed, and IMAP replies now thread correctly for recipients. New option to bring an archived conversation back to the inbox when a reply arrives. The list reveals newly arrived mail when you are at the top, the add-account screen scrolls above the keyboard, the push status line reports the real connection state, and seven more languages catch up.
