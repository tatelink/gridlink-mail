[versions]
# 🔴 agp, kotlin, ksp and room move together or not at all. Kotlin 2.4 needs KSP 2.3, KSP 2.3 refuses
# an AGP below 8.10, and KSP2 cannot run Room 2.6's processor ("unexpected jvm signature V").
# Bumping any one of the four alone fails the build, so a Dependabot PR that touches one is either
# taken with the rest or closed.
# ⛔ androidx core-ktx stays at 1.15.0: 1.19 wants AGP 9.1 and compileSdk 37, which is its own job.
agp = "8.13.2"
kotlin = "2.4.10"
coreKtx = "1.19.0"
lifecycle = "2.8.7"
activityCompose = "1.10.0"
appcompat = "1.8.0"
composeBom = "2025.01.00"
navigation = "2.10.0"
room = "2.8.4"
paging = "3.5.1"
profileinstaller = "1.4.1"
ksp = "2.3.11"
okhttp = "5.4.0"
kotlinxSerialization = "1.11.0"
coroutines = "1.11.0"
biometric = "1.1.0"
fragment = "1.9.0"
datastore = "1.1.1"
work = "2.9.1"
junit = "4.13.2"
robolectric = "4.15.1"
# Static analysis. 1.23.x embeds a Kotlin 1.9 parser, which reads this project's
# Kotlin 2.1 sources fine; revisit if the codebase adopts 2.x-only syntax.
detekt = "1.23.8"
# 3.2.0+ is built with Kotlin 2.3 (metadata unreadable from our Kotlin 2.1);
# bump alongside the project's Kotlin upgrade.
unifiedpush = "3.3.3"
# BouncyCastle, for S/MIME signature VERIFICATION only (CMS SignedData lives in bcpkix,
# the ASN.1 and certificate reading in bcprov). 🔴 Nothing here may call Security.addProvider:
# Android ships a stripped org.bouncycastle of its own under the provider name "BC", and the
# verifier deliberately names no provider at all so the platform's crypto does the work. See
# SmimeVerifier.
# 1.84, not 1.81.1: two moderate advisories land on the older pair (an LDAP injection in
# bcprov's X509LDAPCertStore and a risky algorithm in bcpkix). Neither is on a path this app
# walks — it opens no LDAP store and holds no private key — but taking the fix is free, and an
# advisory left standing on the default branch trains everyone to ignore the next one.
bouncycastle = "1.84"

[libraries]
androidx-core-ktx = { module = "androidx.core:core-ktx", version.ref = "coreKtx" }
androidx-work-runtime-ktx = { module = "androidx.work:work-runtime-ktx", version.ref = "work" }
androidx-work-testing = { module = "androidx.work:work-testing", version.ref = "work" }
androidx-lifecycle-runtime-ktx = { module = "androidx.lifecycle:lifecycle-runtime-ktx", version.ref = "lifecycle" }
androidx-lifecycle-viewmodel-compose = { module = "androidx.lifecycle:lifecycle-viewmodel-compose", version.ref = "lifecycle" }
androidx-lifecycle-runtime-compose = { module = "androidx.lifecycle:lifecycle-runtime-compose", version.ref = "lifecycle" }
androidx-activity-compose = { module = "androidx.activity:activity-compose", version.ref = "activityCompose" }
androidx-appcompat = { module = "androidx.appcompat:appcompat", version.ref = "appcompat" }
androidx-compose-bom = { module = "androidx.compose:compose-bom", version.ref = "composeBom" }
androidx-compose-ui = { module = "androidx.compose.ui:ui" }
androidx-compose-ui-graphics = { module = "androidx.compose.ui:ui-graphics" }
androidx-compose-ui-tooling = { module = "androidx.compose.ui:ui-tooling" }
androidx-compose-ui-tooling-preview = { module = "androidx.compose.ui:ui-tooling-preview" }
androidx-compose-ui-test-junit4 = { module = "androidx.compose.ui:ui-test-junit4" }
androidx-compose-ui-test-manifest = { module = "androidx.compose.ui:ui-test-manifest" }
androidx-compose-material3 = { module = "androidx.compose.material3:material3" }
androidx-compose-material-icons-extended = { module = "androidx.compose.material:material-icons-extended" }
androidx-navigation-compose = { module = "androidx.navigation:navigation-compose", version.ref = "navigation" }
androidx-biometric = { module = "androidx.biometric:biometric", version.ref = "biometric" }
androidx-fragment-ktx = { module = "androidx.fragment:fragment-ktx", version.ref = "fragment" }
androidx-room-runtime = { module = "androidx.room:room-runtime", version.ref = "room" }
androidx-room-ktx = { module = "androidx.room:room-ktx", version.ref = "room" }
androidx-room-compiler = { module = "androidx.room:room-compiler", version.ref = "room" }
androidx-room-paging = { module = "androidx.room:room-paging", version.ref = "room" }
androidx-paging-runtime = { module = "androidx.paging:paging-runtime", version.ref = "paging" }
androidx-paging-compose = { module = "androidx.paging:paging-compose", version.ref = "paging" }
androidx-profileinstaller = { module = "androidx.profileinstaller:profileinstaller", version.ref = "profileinstaller" }
androidx-datastore-preferences = { module = "androidx.datastore:datastore-preferences", version.ref = "datastore" }

# UnifiedPush connector (Apache-2.0, no Google deps — FOSS rule holds). Connector ONLY:
# never add the embedded-fcm-distributor artifacts (they route through Google's FCM).
unifiedpush-connector = { module = "org.unifiedpush.android:connector", version.ref = "unifiedpush" }
okhttp = { module = "com.squareup.okhttp3:okhttp", version.ref = "okhttp" }
okhttp-sse = { module = "com.squareup.okhttp3:okhttp-sse", version.ref = "okhttp" }
okhttp-mockwebserver = { module = "com.squareup.okhttp3:mockwebserver", version.ref = "okhttp" }
kotlinx-serialization-json = { module = "org.jetbrains.kotlinx:kotlinx-serialization-json", version.ref = "kotlinxSerialization" }
kotlinx-coroutines-core = { module = "org.jetbrains.kotlinx:kotlinx-coroutines-core", version.ref = "coroutines" }
kotlinx-coroutines-test = { module = "org.jetbrains.kotlinx:kotlinx-coroutines-test", version.ref = "coroutines" }

bouncycastle-prov = { module = "org.bouncycastle:bcprov-jdk18on", version.ref = "bouncycastle" }
bouncycastle-pkix = { module = "org.bouncycastle:bcpkix-jdk18on", version.ref = "bouncycastle" }

junit = { module = "junit:junit", version.ref = "junit" }
robolectric = { module = "org.robolectric:robolectric", version.ref = "robolectric" }

# Wraps ktlint's formatting rules as detekt rules. Build-time only: it adds no
# runtime dependency and nothing here reaches the APK.
detekt-formatting = { module = "io.gitlab.arturbosch.detekt:detekt-formatting", version.ref = "detekt" }

[plugins]
android-application = { id = "com.android.application", version.ref = "agp" }
android-library = { id = "com.android.library", version.ref = "agp" }
kotlin-android = { id = "org.jetbrains.kotlin.android", version.ref = "kotlin" }
kotlin-jvm = { id = "org.jetbrains.kotlin.jvm", version.ref = "kotlin" }
kotlin-serialization = { id = "org.jetbrains.kotlin.plugin.serialization", version.ref = "kotlin" }
compose-compiler = { id = "org.jetbrains.kotlin.plugin.compose", version.ref = "kotlin" }
ksp = { id = "com.google.devtools.ksp", version.ref = "ksp" }
detekt = { id = "io.gitlab.arturbosch.detekt", version.ref = "detekt" }
